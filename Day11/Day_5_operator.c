#include<stdio.h>

int main()
{
    // float a = 12.45;

    // printf("%f\n",a);
    // printf("%6f\n",a);
    // printf("%-6f\n",a);
    // printf("%06f",a);
    // printf("%6.3f\n",a);

    // int num;
    // char ch ;
    // printf("Enter the number and character : ");
    // scanf("%d%*c%c", &num, &ch);

    // printf("Enter the number : "); 
    // scanf("%d",&num);  // OR
    // scanf("%d%*c",&num);           // %*c -> only for character

    // printf("Enter the character : "); 
    // scanf("%*c%c",&ch);   // OR
    // scanf("%c",&ch); 
   
 // Perimeter of Rectangle
    // double length, breath, perimeter;
    // printf("Enter the length of rectangle : ");
    // scanf("%lf", &length);
    // printf("Enter the breadth of rectangle : ");
    // scanf("%lf",&breath);

    // perimeter = 2 * (length + breath);
    // printf("peremeter = %.3lf", perimeter);


    // Area and circumference of circle

    // float Pi = 3.142;
    // float r, area, circum;

    // printf("Enter the radius of circle : ");
    // scanf("%f",&r);

    // area = 3.142 * r * r;
    // printf("the area of the Circle is : %f\n",area);

    // circum = 2 * 3.142 * r;
    // printf("the circumference of a Circle is : %f\n",circum);


    // int a , b, c;
    // printf("Enter three numbers : ");
    // scanf("%d",&a);
    // scanf("%d",&b);
    // scanf("%d",&c);

    // int avg = (a + b + c) / 2;  // a,b,c = int type
    // printf("Result = %d",avg);  // result = int type 

    // float avg = (a + b + c) / 2.0f;  // a,b,c = int type
    // printf("Result = %f",avg);       // result = float type

    // double avg = (a + b + c) / 2.0;  // a,b,c = int type
    // printf("Result = %f",avg);       // result = double type

    // float avg = ((float)a + b + c) / 2;  // a,b,c = int type
    // printf("Result = %f",avg);           // result = float type

    // int a = (float)a;  // type casting 

    return 0;
}